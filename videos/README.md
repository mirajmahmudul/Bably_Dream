Drop your own clips here, named to match App.tsx:

- studio.mp4
- about.mp4
- journal.mp4
- reach-us.mp4

(home.mp4 isn't required — that section currently points at the
Aethera CloudFront URL directly; swap it for a local file the
same way once you have one.)

Any section without a matching file just shows its fallback
gradient color — nothing breaks, so you can add clips one at a time.
